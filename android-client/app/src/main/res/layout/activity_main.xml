<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        tools:context=".MainActivity">

        <include
            android:id="@+id/layout_calculate"
            layout="@layout/layout_calculate" />

        <include
            android:id="@+id/layout_fibonacci"
            layout="@layout/layout_fibonacci" />

        <include
            android:id="@+id/layout_sum_current"
            layout="@layout/layout_sum_current" />

        <include
            android:id="@+id/layout_sum_total"
            layout="@layout/layout_sum_total" />

    </LinearLayout>

</ScrollView>
